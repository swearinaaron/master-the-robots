{
  "name": "master-the-robots",
  "version": "1.0.0",
  "description": "Master the Robots - AI & Robotics Education Platform",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "cd server && npm install && npm run build && node dist/index.js",
    "build": "cd server && npm install && npm run build",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  },
  "cacheDirectories": [
    "server/node_modules"
  ]
}
