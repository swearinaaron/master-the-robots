{
  "name": "master-the-robots",
  "version": "1.0.0",
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:staging": "tsc && vite build --mode staging",
    "build:prod": "tsc && vite build --mode production",
    "preview": "vite preview"
  },
  "dependencies": {
    "@types/node": "^18.11.9",
    "typescript": "^4.9.5",
    "vite": "^4.5.5"
  }
}
