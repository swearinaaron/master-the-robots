{
  "name": "master-the-robots",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "cd server && node dist/index.js",
    "build": "cd server && npm install && tsc",
    "heroku-postbuild": "cd server && npm install && tsc"
  }
}
